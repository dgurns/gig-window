name: Deploy to Staging

on:
  pull_request:
    types: [opened, synchronize]

# on:
#   push:
#     branches:
#       - develop

jobs:
  build-push-update:
    name: Build images, push to Docker Hub, and update staging server
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Docker QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and push `nginx` Docker image
        uses: docker/build-push-action@v2
        with:
          file: ./nginx/Dockerfile
          push: true
          tags: |
            dgurney/gig-window-nginx:latest
            dgurney/gig-window-nginx:${{ github.sha }}
          secrets: |
            GIT_AUTH_TOKEN=${{ github.token }}

      - name: Build and push `api` Docker image
        uses: docker/build-push-action@v2
        with:
          file: ./api/Dockerfile
          push: true
          tags: |
            dgurney/gig-window-api:latest
            dgurney/gig-window-api:${{ github.sha }}
          secrets: |
            GIT_AUTH_TOKEN=${{ github.token }}

      # - name: Run `start.staging.sh` on staging server
      #   uses: garygrossgarten/github-action-ssh@release
      #   with:
      #     command: sh start.staging.sh
      #     host: api-staging.gigwindow.com
      #     username: root
      #     privateKey: ${{ secrets.PRIVATE_SSH_KEY }}
